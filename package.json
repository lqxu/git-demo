{
  "name": "gulp-demo",
  "version": "1.0.0",
  "description": "1. 命令行创建 npm 配置文件  `npm init` 2. 添加一个gulp的依赖  `npm install gulp --save-dev` 3. 在项目根目录下添加一个gulpfile.js文件，这个是gulp的主文件，这个文件名是固定的 4. 在gulpfile中定义我们需要做的任务 task",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.8",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-cssnano": "^2.1.2",
    "gulp-htmlmin": "^3.0.0",
    "gulp-less": "^3.3.0",
    "gulp-uglify": "^2.1.0"
  }
}
